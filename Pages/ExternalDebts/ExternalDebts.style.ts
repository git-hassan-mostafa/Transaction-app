import Constants from "@/Shared/Constants/Constants";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  fab: {
    backgroundColor: Constants.colors.externalDebts,
  },
});

export default styles;
